---
layout: post
title: "基础 · 扫描线"
date: 2024-08-01 12:00:00 +0800
tags: [algorithm, sweep-line]
excerpt: "把区间拆成进出事件，按坐标排序后一遍扫描处理重叠与覆盖问题。"
---

扫描线是用来求重叠区域的个数这类问题。

很经典的题就是求某一个时刻公交车上最多的人数，公交车的人上上下下，上车就+1，下车就-1，自然就能求出最多的人数。

这类问题基本上都是公交车问题的变种。

按时间排序也是求解问题的前提之一。

## [253. Meeting Rooms II](https://leetcode.cn/problems/meeting-rooms-ii/)
<!-- 这是一张图片，ocr 内容为：[[1,4],[2,6],[3,7],[4,5]] RETURN 碰到INTERVAL的START,也就是起飞一架飞机, MAX: 当前天上的飞机数++. 碰到INTERVAL的END,也就是降落一架飞机, 当前天上的飞机数--. 我们分别把所有的START和所有的END放进两 个数组,并排序. O 然后从第一个START开始统计,碰到START就加 一,碰到END就减一.并且同时维护一个最 大飞机数的MAX. 注意图上在4这个位置,有一上一下.题目 说先算降落的再算起飞的. 当所有的START统计过以后,我们就有答案 了.后面的END只会往下减,所以不用再看 了. 123456 -->
![](https://cdn.nlark.com/yuque/0/2024/png/32473878/1715851266031-a599e0a5-3fe4-4550-87bf-9efec17aee1e.png)

实际上就是在求重复区域的最大重叠数，思路可以用扫描线，排序后然后起点+1，终点-1，累计起来的就是答案。

```java
class Solution {
  public int minMeetingRooms(int[][] intervals) {
    // 实际上是在求重复区域的最大重叠数
    Arrays.sort(intervals, (a, b) -> a[0] != b[0] ? a[0] - b[0] : a[1] - b[1]);
    List<int[]> lines = new ArrayList<>();

    for (int i = 0; i < intervals.length; i++) {
      // 1 代表进入， -1 代表退出

      lines.add(new int[] { intervals[i][0], 1 });
      lines.add(new int[] { intervals[i][1], -1 });
    }
    // [[0,30],[5,10],[10,20]]
    // [0, 1] [5, 1] [10, -1] [10, 1] [20, -1] [30, -1]
    // 所以要保证先退出，再进入，否则出现这种[0, 1] [5, 1] [10, 1] [10, -1]就会比实际情况多一人
    Collections.sort(lines, (a, b) -> a[0] != b[0] ? a[0] - b[0] : a[1] - b[1]);
    int cnt = 0;
    int max = 0;
    for (int[] line : lines) {
      cnt += line[1];
      max = Math.max(max, cnt);
    }
    return max;
  }
}
```

## xxx
```java
class Solution {
    public int minAbsoluteDifference(List<Integer> nums, int x) {
      int len = nums.size();
      List<int[]> line = new ArrayList<>(len);
      for(int i = 0; i < len; i++) {
        line.add(new int[]{nums.get(i), i});
      }
      Collections.sort(line, (a, b) -> a[0] - b[0]);
      
      int min = Integer.MAX_VALUE;
      for(int i = 1; i < len; i++) {
        int[] pre = line.get(i - 1);
        int[] next = line.get(i);
        System.out.println("pre: " + pre[0] + " "+ pre[1]);
        System.out.println("next: " + next[0]  + " "+ next[1]);
        
        if(Math.abs(next[1] - pre[1]) >= x) {
          min = Math.min(min, Math.abs(next[0] - pre[0]));
        }
      }
      return min;
      

    }
}
```

## [56. Merge Intervals](https://leetcode.cn/problems/merge-intervals/)
注意这里是根据cnt为0去除重复的，所以排序跟253题又不一样，253题是先退出再进入来保证某一刻计数的人数是正确的。

本题是要先进入再退出，保证同一相同的端点会被去除。

```java
class Solution {
  public int[][] merge(int[][] intervals) {
    // 扫描线
    List<int[]> lines = new ArrayList<>();
    for (int[] interval : intervals) {
      lines.add(new int[] { interval[0], 1 });
      lines.add(new int[] { interval[1], -1 });
    }
    // [[1,4],[4,5]]
    // 扫描线：[1, 1] [4, 1] [4, -1] [5, -1]
    // 注意这里 [4, 1] [4, -1]
    // 的顺序，因为[[1,4],[4,5]]在端点4也是被看作重复的，去除重复是通过计数为0，如果要去除重复的，得保证1在前面
    Collections.sort(lines, (a, b) -> a[0] != b[0] ? a[0] - b[0] : b[1] - a[1]);

    int cnt = 0;
    int left = 0;
    List<int[]> res = new ArrayList<>();
    for (int right = 0; right < lines.size(); right++) {
      int[] line = lines.get(right);
      cnt += line[1];
      if (cnt == 0) {
        res.add(new int[] { lines.get(left)[0], lines.get(right)[0] });
        left = right + 1;
      }
    }
    int[][] meeting = new int[res.size()][2];
    for (int i = 0; i < res.size(); i++) {
      meeting[i][0] = res.get(i)[0];
      meeting[i][1] = res.get(i)[1];
    }
    return meeting;
  }
}Ï
```

### 双指针解法
```java
class Solution {
  public int[][] merge(int[][] intervals) {

    Arrays.sort(intervals, (a, b) -> a[0] != b[0] ? a[0] - b[0] : a[1] - b[1]);
    List<int[]> res = new ArrayList<>();
    int i = 0;
    while(i < intervals.length) {
      int start = intervals[i][0];
      int end = intervals[i][1];
      i++;
      // 当前间隔的end比下一个间隔的start还大时，说明是可以合并的。
      while(i < intervals.length && end >= intervals[i][0]) {
        end = Math.max(end, intervals[i][1]);
        i++;
      }
      res.add(new int[]{start, end});
    }
    return res.toArray(int[][]::new);
  }
}
```

## [2779. 数组的最大美丽值](https://leetcode.cn/problems/maximum-beauty-of-an-array-after-applying-operation/)
<!-- 这是一张图片，ocr 内容为：PROBLEM B数组的最大美丽值 解法:排序 元素Z可以变成[Z-K.2+引里的任意一个数,那么区问重叠的最多次数就是答案. 求区间重叠的最多次取是一个经典问题,见LEETOODE253.会议室I.做法就是将区间(.门看成两个 事件:位于1的区间加入事件,和位于,的区间离开事件.依此处理事件,看最多同时有几个区间 加入即可,注意,因为区问是闭区问,所以对于相同位置的事件,要先处理区问加入,再处理区间 离开 复杂度(R LOGN),主要是排序的复杂度, 参考代码(C++) PUBIEC: INT WAXIMINBEAUTY(VECTOR<INT> NUMS,INT K)[ TYPEDEF PAIRCINT,INT> PIZ VECTOR<PII> VEC; 1/第二位约.0表示区间LO入事件,1表示区间离开事件 I/这样绘阿一个位置的事件排序时,因为  持在1 的面,就能先处理区闲加入 FOR (INT X I NUMS) VEC.PUSH BACKIPII(X - K,03),VEC.PUSH BACK(PIL(X + K,I) SORT(VEC.BEGIN(),VEE.END()); //NOW:当前有几个区间 INT ANG                                                                                               /依此处理事件 FOR (PIS P I VEC) 1/区间加入事件 101 IF(P.SECOND //区间离开事件 //更新答案 -->
![](https://cdn.nlark.com/yuque/0/2024/png/32473878/1715851373313-b554c3d7-cb85-440c-8ea4-da071c270e04.png)

因为最后子序列是相同的数字，所以不需要考虑顺序。只需要考虑最后同时能为某个数字。

```java
class Solution {
  public int maximumBeauty(int[] nums, int k) {
    List<int[]> line = new ArrayList<>();

    for(int i = 0; i < nums.length; i++) {
      line.add(new int[]{nums[i] - k, 1});
      line.add(new int[]{nums[i] + k, -1});
    }
    Collections.sort(line, (a, b) -> a[0] != b[0] ? a[0] - b[0] : b[1] - a[1]);

    int ans = 0;
    int max = 0;
    for(int i = 0; i < line.size(); i++) {
      int[] range = line.get(i);
      ans += range[1];
      max = Math.max(max, ans);
    }
    return max;


  }
}
```

**其他方法**

```java
class Solution {
  public int maximumBeauty(int[] nums, int k) {
    Arrays.sort(nums);
    int max = 0;
    for (int i = 0, j = 0; i < nums.length; i++) {
      for (; j < nums.length && nums[j] - nums[i] <= 2 * k; j++) {
      }
      max = Math.max(max, j - i);
    }
    return max;
  }
}
```
