# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you:
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: Shawn's Notebook
email: ""
description: >-
  Notes on experiments in tech and learning from Shawn Xiao.
baseurl: ""
url: "https://shawnxiaozh.github.io"
twitter_username: ""
github_username: shawnxiaozh
show_excerpts: true
future: true

# 文章 URL 用标题 slug，不含日期（文件名前的 YYYY-MM-DD- 只是 Jekyll 识别 post 的必需前缀，
# 不参与 URL）。这样以后不管 front matter 里的 date 怎么随机改，线上链接都不会跟着变。
permalink: /:title/

navigation:
  - title: Home
    url: /
  - title: Articles
    url: /articles/
  - title: Tags
    url: /tags/
  - title: About
    url: /about/

# Build settings
theme: jekyll-theme-cayman
plugins:
  - jekyll-feed
  - jekyll-remote-theme

# 文章正文跳过 Liquid 渲染（Jekyll 4.0+）。
# 笔记里的代码常出现 {{ 、{% 等字符（如 Java 的二维数组 {{1,0}, {0,1}}），
# 而 Liquid 在 Markdown 之前运行、不认识代码围栏，会把它们当成模板标签导致构建失败。
# 关掉后正文就是纯 Markdown，无需 {% raw %} 包裹；layout 渲染不受影响。
# 代价：文章正文里无法再使用 {{ site.xxx }} 这类 Liquid 变量。
defaults:
  - scope:
      path: "_posts"
      type: "posts"
    values:
      render_with_liquid: false

# 不要把仓库里的开发辅助文件发布到站点
exclude:
  - CLAUDE.md
  - Gemfile
  - Gemfile.lock
  - vendor/

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
# exclude:
#   - .sass-cache/
#   - .jekyll-cache/
#   - gemfiles/
#   - Gemfile
#   - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
